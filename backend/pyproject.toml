[build-system]
requires = ["setuptools>=64.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "upscaler"
version = "1.0.0"

dependencies = [
	"fastapi",
	"uvicorn",
	"gunicorn",
	"python-dotenv",
	"python-multipart"
]
